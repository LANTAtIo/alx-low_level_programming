#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - numbers of base 10
 *
 * Return: Always 0 (Success))
 */
int main(void)

{
	int bas;

	for (bas = 0; bas < 10; bas++)
		printf("%d", bas);
	printf("\n");

	return (0);

}
