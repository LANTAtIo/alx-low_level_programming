#include "main.h"
#include <stdio.h>
/**
 * unction appends the src string to the dest string
 * Return! Always 0
 */
char *_strcat(char *dest, char *src)
{
	char *dest[];
	char *src[];

	strcat(dest, src);

	printf("%s", dest);
}
